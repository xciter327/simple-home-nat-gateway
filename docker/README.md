# Install Docker-CE and disable firewall management, we'll be using `host_mode` only

We'll also not be adding a separate `docker` group, since granting access to this group will be effectively granting root

